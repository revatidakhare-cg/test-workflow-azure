# Azure App Service security baseline policies
# HTTPS-only and minimum TLS 1.2 are enforced via site_config in main.tf.
# SSL certificates are managed via Key Vault references.
# Diagnostic settings are configured in main.tf.

