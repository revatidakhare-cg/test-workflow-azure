# App Service Network Access Rules
# Private endpoints and NSG rules are managed at the network layer (nsg module).
# VNet integration should be configured by the operator as needed via azurerm_app_service_virtual_network_swift_connection.

